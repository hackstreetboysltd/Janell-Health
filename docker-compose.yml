services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: carelink
      POSTGRES_PASSWORD: carelink
      POSTGRES_DB: carelink
    ports:
      - "5432:5432"
    volumes:
      - carelink_pg:/var/lib/postgresql/data

volumes:
  carelink_pg:
